# DISTRIBUTED LINKS
#
# - Use <node ...> for remote server endpoints.
# - Optional role= values: distributed, slave, distributed,slave.
# - role="master" is accepted as an alias for distributed.
# - Legacy <slave ...> tags still work, but role= on <node> is preferred.
# - Use passwd/pass/password/token as peer auth aliases.
#
# This file should list remote peers only (no self-node entry)

<node
     host="127.0.0.1"
     port="9201"
     role="distributed,slave"
     passwd="XBi0exfFbWo7crXhFbkF">

<node
     host="127.0.0.1"
     port="9202"
     role="distributed"
     passwd="XBi0exfFbWo7crXhFbkF">

<replication
     enabled="true"
     mode="sync_one"
     timeout_ms="2000">

<distributed_search
     enabled="true"
     mode="local_first"
     prefer_local="true"
     timeout_ms="250"
     persistent="true"
     burst="64"
     autoconnect="true"
     reconnect_ms="100">
